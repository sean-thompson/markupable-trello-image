name: Build
on:
  push:
    branches: [ main ]
jobs:
  Build:
    runs-on: ubuntu-latest
    env:
      NODE_ENV: production
      PORT: 3000
      POWERUP_NAME: My Power-Up
      POWERUP_URL: https://markupable-trello.pages.dev
      CONTEXT_PATH: /
      POWERUP_ID: UNSPECIFIED
      POWERUP_APP_KEY: ${{ secrets.POWERUP_APP_KEY }}
      TRELLO_IMAGE_PROXY_URL: ${{ vars.TRELLO_IMAGE_PROXY_URL }}
      OPTRO_API_KEY: DISABLED
      OPTRO_LICENSE_TYPE: UNSPECIFIED
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Packages
        run: yarn install --production=false
      - name: Build
        run: yarn build
      - name: Deploy to Cloudflare Pages
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx wrangler@3 pages deploy dist --project-name=markupable-trello
